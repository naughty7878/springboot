spring:
  datasource:
    username: hd
    password: hd123456
    url: jdbc:mysql://47.106.74.177:13306/test_mybatis_plus?allowPublicKeyRetrieval=true&useSSL=true
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    # 日志输出SQL
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
#mybatis:
#  # 指定全局配置文件位置
#  config-location: classpath:mybatis/mybatis-config.xml
#  # 指定sql映射文件位置
#  mapper-locations: classpath:mybatis/mapper/*.xml